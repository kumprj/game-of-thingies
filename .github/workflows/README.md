# Clean Up Lambda

Python Lambda that is deployed with Terraform to clean up the old Games, the Games' Entries, and Player Scores.

# AWS Amplify
amplify.yml in the root deploys the client front-end.

# Server Side
The server is deployed as a Render app, hooked up to the main branch. 